<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".DBuggingApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication"
        android:usesCleartextTraffic="true">
        <activity android:name=".presentation.login.LoginActivity">


        </activity>
        <activity android:name=".presentation.register.RegisterActivity" />
        <activity android:name=".presentation.home.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".presentation.company.CompanyActivity" />
        <activity android:name=".presentation.mypage.MyPageActivity" />
        <activity android:name=".presentation.product.ProductActivity" />
        <activity android:name=".presentation.survey.SurveyActivity" />
        <activity android:name=".presentation.bug.BugActivity" />
    </application>

</manifest>